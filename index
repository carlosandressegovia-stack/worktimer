<!DOCTYPE html>
<html lang="es">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover">
<title>Workout Timer</title>
<link href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
<style>
*, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

:root {
  --glass-bg: rgba(255,255,255,0.18);
  --glass-bg-strong: rgba(255,255,255,0.28);
  --glass-border: rgba(255,255,255,0.45);
  --glass-shadow: 0 8px 32px rgba(0,0,0,0.18), 0 1.5px 0 rgba(255,255,255,0.35) inset;
  --glass-shadow-sm: 0 4px 16px rgba(0,0,0,0.12), 0 1px 0 rgba(255,255,255,0.3) inset;
  --blur: blur(24px) saturate(180%);
  --blur-sm: blur(12px) saturate(160%);
  --accent: #30d158;
  --accent2: #0a84ff;
  --accent3: #ff6b35;
  --danger: #ff453a;
  --text: #1a1a2e;
  --text-soft: rgba(26,26,46,0.6);
  --text-white: rgba(255,255,255,0.95);
  --text-white-soft: rgba(255,255,255,0.6);
  --radius-sm: 12px;
  --radius-md: 18px;
  --radius-lg: 26px;
  --radius-xl: 36px;
  --font: 'Outfit', sans-serif;
}

html, body {
  height: 100%;
  font-family: var(--font);
  overflow: hidden;
}

body {
  background: linear-gradient(135deg, #0f0c29 0%, #302b63 40%, #24243e 70%, #0f3460 100%);
  min-height: 100vh;
  position: relative;
}

/* Orbs de fondo */
body::before {
  content: '';
  position: fixed;
  width: 600px; height: 600px;
  background: radial-gradient(circle, rgba(48,209,88,0.25) 0%, transparent 70%);
  top: -200px; right: -200px;
  border-radius: 50%;
  pointer-events: none;
  animation: orbFloat 8s ease-in-out infinite;
}
body::after {
  content: '';
  position: fixed;
  width: 500px; height: 500px;
  background: radial-gradient(circle, rgba(10,132,255,0.2) 0%, transparent 70%);
  bottom: -150px; left: -150px;
  border-radius: 50%;
  pointer-events: none;
  animation: orbFloat 10s ease-in-out infinite reverse;
}
@keyframes orbFloat {
  0%,100% { transform: translate(0,0) scale(1); }
  50% { transform: translate(30px,-30px) scale(1.05); }
}

/* ══════════ APP SHELL ══════════ */
#app {
  height: 100dvh;
  display: flex;
  flex-direction: column;
  max-width: 480px;
  margin: 0 auto;
  position: relative;
  z-index: 1;
}

/* ══════════ SCREENS ══════════ */
.screen {
  display: none;
  flex-direction: column;
  flex: 1;
  overflow: hidden;
  animation: fadeIn 0.3s ease;
}
.screen.active { display: flex; }
@keyframes fadeIn { from { opacity: 0; transform: translateY(10px); } to { opacity: 1; transform: translateY(0); } }

.screen-body {
  flex: 1;
  overflow-y: auto;
  padding: 0 16px 100px;
  scrollbar-width: none;
}
.screen-body::-webkit-scrollbar { display: none; }

/* ══════════ GLASS CARD ══════════ */
.glass {
  background: var(--glass-bg);
  backdrop-filter: var(--blur);
  -webkit-backdrop-filter: var(--blur);
  border: 1px solid var(--glass-border);
  box-shadow: var(--glass-shadow);
  border-radius: var(--radius-lg);
}
.glass-sm {
  background: var(--glass-bg);
  backdrop-filter: var(--blur-sm);
  -webkit-backdrop-filter: var(--blur-sm);
  border: 1px solid var(--glass-border);
  box-shadow: var(--glass-shadow-sm);
  border-radius: var(--radius-md);
}
.glass-strong {
  background: var(--glass-bg-strong);
  backdrop-filter: var(--blur);
  -webkit-backdrop-filter: var(--blur);
  border: 1px solid var(--glass-border);
  box-shadow: var(--glass-shadow);
  border-radius: var(--radius-lg);
}

/* ══════════ TOP BAR ══════════ */
.topbar {
  padding: 56px 20px 16px;
  display: flex;
  align-items: center;
  justify-content: space-between;
  flex-shrink: 0;
}
.topbar-title {
  font-size: 28px;
  font-weight: 700;
  color: var(--text-white);
  letter-spacing: -0.5px;
}
.topbar-sub {
  font-size: 13px;
  color: var(--text-white-soft);
  font-weight: 400;
  margin-top: 2px;
}
.icon-btn {
  width: 40px; height: 40px;
  background: var(--glass-bg);
  backdrop-filter: var(--blur-sm);
  -webkit-backdrop-filter: var(--blur-sm);
  border: 1px solid var(--glass-border);
  border-radius: 50%;
  display: flex; align-items: center; justify-content: center;
  cursor: pointer;
  color: var(--text-white);
  transition: all 0.2s;
  flex-shrink: 0;
}
.icon-btn:hover { background: var(--glass-bg-strong); transform: scale(1.05); }
.icon-btn svg { width: 18px; height: 18px; }

/* ══════════ BOTTOM NAV ══════════ */
.bottom-nav {
  position: fixed;
  bottom: 0; left: 50%;
  transform: translateX(-50%);
  width: 100%;
  max-width: 480px;
  padding: 0 16px 28px;
  z-index: 100;
}
.nav-bar {
  background: rgba(255,255,255,0.15);
  backdrop-filter: blur(30px) saturate(200%);
  -webkit-backdrop-filter: blur(30px) saturate(200%);
  border: 1px solid rgba(255,255,255,0.3);
  box-shadow: 0 -1px 0 rgba(255,255,255,0.2) inset, 0 8px 32px rgba(0,0,0,0.3);
  border-radius: 28px;
  display: flex;
  align-items: center;
  justify-content: space-around;
  padding: 10px 8px;
}
.nav-item {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 3px;
  cursor: pointer;
  padding: 6px 16px;
  border-radius: 18px;
  transition: all 0.2s;
  color: var(--text-white-soft);
  min-width: 60px;
}
.nav-item.active {
  background: rgba(48,209,88,0.2);
  color: var(--accent);
}
.nav-item svg { width: 22px; height: 22px; }
.nav-label { font-size: 10px; font-weight: 600; letter-spacing: 0.3px; }

/* ══════════ SCREEN: HOME ══════════ */
.greeting-card {
  padding: 20px 22px;
  margin-bottom: 16px;
}
.greeting-text {
  font-size: 15px;
  color: var(--text-white-soft);
  font-weight: 400;
  margin-bottom: 4px;
}
.greeting-name {
  font-size: 22px;
  font-weight: 700;
  color: var(--text-white);
  letter-spacing: -0.3px;
}
.streak-row {
  display: flex;
  gap: 10px;
  margin-top: 14px;
}
.streak-pill {
  display: flex;
  align-items: center;
  gap: 6px;
  background: rgba(48,209,88,0.15);
  border: 1px solid rgba(48,209,88,0.3);
  border-radius: 20px;
  padding: 5px 12px;
  font-size: 12px;
  font-weight: 600;
  color: var(--accent);
}
.streak-pill svg { width: 14px; height: 14px; }

.section-label {
  font-size: 13px;
  font-weight: 600;
  color: var(--text-white-soft);
  letter-spacing: 0.8px;
  text-transform: uppercase;
  margin: 20px 0 10px;
  padding: 0 4px;
}

.rutina-card {
  padding: 18px 20px;
  margin-bottom: 10px;
  cursor: pointer;
  transition: all 0.25s;
  display: flex;
  align-items: center;
  justify-content: space-between;
}
.rutina-card:hover { transform: translateY(-2px); background: var(--glass-bg-strong); }
.rutina-card:active { transform: scale(0.98); }
.rutina-info { flex: 1; }
.rutina-name {
  font-size: 17px;
  font-weight: 600;
  color: var(--text-white);
  margin-bottom: 4px;
}
.rutina-meta {
  font-size: 12px;
  color: var(--text-white-soft);
  font-weight: 400;
}
.rutina-arrow {
  width: 32px; height: 32px;
  background: rgba(48,209,88,0.2);
  border: 1px solid rgba(48,209,88,0.3);
  border-radius: 50%;
  display: flex; align-items: center; justify-content: center;
  color: var(--accent);
  flex-shrink: 0;
}
.rutina-arrow svg { width: 16px; height: 16px; }

.empty-state {
  text-align: center;
  padding: 40px 20px;
  color: var(--text-white-soft);
}
.empty-state svg { width: 48px; height: 48px; margin-bottom: 12px; opacity: 0.4; }
.empty-state p { font-size: 14px; font-weight: 400; }

/* ══════════ SCREEN: TIMER ══════════ */
#screen-timer .topbar { padding-top: 56px; }

.timer-header {
  text-align: center;
  padding: 0 20px 20px;
}
.timer-rutina-name {
  font-size: 22px;
  font-weight: 700;
  color: var(--text-white);
  letter-spacing: -0.3px;
}
.timer-set-badge {
  display: inline-block;
  background: rgba(10,132,255,0.2);
  border: 1px solid rgba(10,132,255,0.35);
  color: #64b5ff;
  font-size: 12px;
  font-weight: 600;
  padding: 3px 12px;
  border-radius: 20px;
  margin-top: 6px;
}

.timer-ring-wrap {
  display: flex;
  justify-content: center;
  padding: 10px 20px 20px;
}
.timer-ring-container {
  position: relative;
  width: 240px; height: 240px;
}
.timer-ring-container svg {
  position: absolute;
  top: 0; left: 0;
  width: 100%; height: 100%;
  transform: rotate(-90deg);
}
.ring-bg { fill: none; stroke: rgba(255,255,255,0.1); stroke-width: 8; }
.ring-progress {
  fill: none;
  stroke: var(--accent);
  stroke-width: 8;
  stroke-linecap: round;
  transition: stroke-dashoffset 1s linear, stroke 0.3s;
  filter: drop-shadow(0 0 8px rgba(48,209,88,0.6));
}
.ring-progress.rest { stroke: var(--accent2); filter: drop-shadow(0 0 8px rgba(10,132,255,0.6)); }
.timer-ring-inner {
  position: absolute;
  inset: 0;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
}
.timer-countdown {
  font-size: 54px;
  font-weight: 700;
  color: var(--text-white);
  letter-spacing: -2px;
  line-height: 1;
}
.timer-ejercicio-name {
  font-size: 13px;
  font-weight: 500;
  color: var(--text-white-soft);
  text-align: center;
  padding: 0 20px;
  margin-top: 4px;
  max-width: 160px;
}

.timer-ejercicio-card {
  margin: 0 20px 16px;
  padding: 16px 20px;
  text-align: center;
}
.timer-ej-actual {
  font-size: 20px;
  font-weight: 700;
  color: var(--text-white);
  letter-spacing: -0.3px;
  margin-bottom: 4px;
}
.timer-ej-siguiente {
  font-size: 13px;
  color: var(--text-white-soft);
}
.timer-ej-siguiente span {
  color: var(--accent);
  font-weight: 600;
}

.timer-progress-bar-wrap {
  padding: 0 20px 16px;
}
.timer-progress-label {
  display: flex;
  justify-content: space-between;
  font-size: 12px;
  color: var(--text-white-soft);
  font-weight: 500;
  margin-bottom: 8px;
}
.timer-progress-bar {
  height: 6px;
  background: rgba(255,255,255,0.1);
  border-radius: 3px;
  overflow: hidden;
}
.timer-progress-fill {
  height: 100%;
  background: linear-gradient(90deg, var(--accent2), var(--accent));
  border-radius: 3px;
  transition: width 1s linear;
}

.timer-controls {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 16px;
  padding: 0 20px 16px;
}
.btn-control {
  width: 52px; height: 52px;
  border-radius: 50%;
  border: none;
  cursor: pointer;
  display: flex; align-items: center; justify-content: center;
  transition: all 0.2s;
  font-family: var(--font);
}
.btn-control.secondary {
  background: rgba(255,255,255,0.12);
  backdrop-filter: var(--blur-sm);
  -webkit-backdrop-filter: var(--blur-sm);
  border: 1px solid rgba(255,255,255,0.25);
  color: var(--text-white);
}
.btn-control.secondary:hover { background: rgba(255,255,255,0.2); transform: scale(1.05); }
.btn-control.primary {
  width: 70px; height: 70px;
  background: linear-gradient(135deg, var(--accent), #25a244);
  box-shadow: 0 4px 20px rgba(48,209,88,0.5);
  color: white;
}
.btn-control.primary:hover { transform: scale(1.05); box-shadow: 0 6px 28px rgba(48,209,88,0.65); }
.btn-control svg { width: 24px; height: 24px; }
.btn-control.primary svg { width: 30px; height: 30px; }

.timer-queue {
  margin: 0 20px 16px;
  padding: 14px 16px;
}
.queue-title {
  font-size: 11px;
  font-weight: 600;
  color: var(--text-white-soft);
  letter-spacing: 0.8px;
  text-transform: uppercase;
  margin-bottom: 10px;
}
.queue-item {
  display: flex;
  align-items: center;
  gap: 10px;
  padding: 7px 0;
  border-bottom: 1px solid rgba(255,255,255,0.06);
}
.queue-item:last-child { border-bottom: none; }
.queue-item.current { opacity: 1; }
.queue-item.done { opacity: 0.35; }
.queue-item.upcoming { opacity: 0.6; }
.queue-dot {
  width: 8px; height: 8px;
  border-radius: 50%;
  flex-shrink: 0;
}
.queue-dot.active { background: var(--accent); box-shadow: 0 0 6px var(--accent); }
.queue-dot.rest-dot { background: var(--accent2); }
.queue-dot.done-dot { background: rgba(255,255,255,0.3); }
.queue-dot.up-dot { background: rgba(255,255,255,0.2); }
.queue-item-name {
  flex: 1;
  font-size: 13px;
  font-weight: 500;
  color: var(--text-white);
}
.queue-item-time {
  font-size: 12px;
  color: var(--text-white-soft);
  font-weight: 600;
}

/* ══════════ SCREEN: HISTORIAL ══════════ */
.stats-row {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 10px;
  margin-bottom: 16px;
}
.stat-card {
  padding: 16px 18px;
}
.stat-value {
  font-size: 28px;
  font-weight: 700;
  color: var(--text-white);
  letter-spacing: -1px;
  line-height: 1;
}
.stat-label {
  font-size: 12px;
  color: var(--text-white-soft);
  font-weight: 400;
  margin-top: 4px;
}
.stat-accent { color: var(--accent); }

.hist-item {
  padding: 14px 18px;
  margin-bottom: 8px;
  display: flex;
  align-items: center;
  gap: 14px;
}
.hist-dot {
  width: 42px; height: 42px;
  border-radius: 14px;
  background: rgba(48,209,88,0.15);
  border: 1px solid rgba(48,209,88,0.25);
  display: flex; align-items: center; justify-content: center;
  flex-shrink: 0;
  font-size: 18px;
}
.hist-info { flex: 1; }
.hist-rutina {
  font-size: 15px;
  font-weight: 600;
  color: var(--text-white);
  margin-bottom: 3px;
}
.hist-fecha {
  font-size: 12px;
  color: var(--text-white-soft);
}
.hist-duracion {
  font-size: 14px;
  font-weight: 700;
  color: var(--accent);
}
.hist-notas {
  font-size: 11px;
  color: var(--text-white-soft);
  font-style: italic;
  margin-top: 2px;
}

/* ══════════ SCREEN: CALENDARIO ══════════ */
.cal-nav {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 0 20px 16px;
}
.cal-month {
  font-size: 20px;
  font-weight: 700;
  color: var(--text-white);
  letter-spacing: -0.3px;
}
.cal-card {
  margin: 0 16px 16px;
  padding: 20px;
}
.cal-days-header {
  display: grid;
  grid-template-columns: repeat(7, 1fr);
  text-align: center;
  margin-bottom: 10px;
}
.cal-day-header {
  font-size: 11px;
  font-weight: 600;
  color: var(--text-white-soft);
  letter-spacing: 0.5px;
  text-transform: uppercase;
  padding: 4px 0;
}
.cal-grid {
  display: grid;
  grid-template-columns: repeat(7, 1fr);
  gap: 4px;
}
.cal-cell {
  aspect-ratio: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  border-radius: 10px;
  position: relative;
  cursor: default;
}
.cal-cell-num {
  font-size: 13px;
  font-weight: 500;
  color: var(--text-white-soft);
  line-height: 1;
}
.cal-cell.has-workout {
  background: rgba(48,209,88,0.15);
  border: 1px solid rgba(48,209,88,0.25);
  cursor: pointer;
}
.cal-cell.has-workout .cal-cell-num { color: var(--accent); font-weight: 700; }
.cal-cell.today {
  background: rgba(10,132,255,0.2);
  border: 1px solid rgba(10,132,255,0.4);
}
.cal-cell.today .cal-cell-num { color: #64b5ff; font-weight: 700; }
.cal-cell.other-month .cal-cell-num { opacity: 0.25; }
.cal-dot {
  width: 4px; height: 4px;
  border-radius: 50%;
  background: var(--accent);
  margin-top: 2px;
}

.cal-detail {
  margin: 0 16px 16px;
  padding: 16px 20px;
}
.cal-detail-title {
  font-size: 13px;
  font-weight: 600;
  color: var(--text-white-soft);
  text-transform: uppercase;
  letter-spacing: 0.8px;
  margin-bottom: 12px;
}
.cal-detail-item {
  display: flex;
  align-items: center;
  gap: 12px;
  padding: 8px 0;
  border-bottom: 1px solid rgba(255,255,255,0.06);
}
.cal-detail-item:last-child { border-bottom: none; }

/* ══════════ SCREEN: EDITOR ══════════ */
.editor-rutina-select {
  margin: 0 16px 16px;
}
.form-group { margin-bottom: 14px; }
.form-label {
  font-size: 12px;
  font-weight: 600;
  color: var(--text-white-soft);
  letter-spacing: 0.8px;
  text-transform: uppercase;
  display: block;
  margin-bottom: 6px;
}
.form-input {
  width: 100%;
  background: rgba(255,255,255,0.1);
  backdrop-filter: var(--blur-sm);
  -webkit-backdrop-filter: var(--blur-sm);
  border: 1px solid rgba(255,255,255,0.2);
  border-radius: var(--radius-sm);
  padding: 12px 14px;
  font-family: var(--font);
  font-size: 15px;
  font-weight: 500;
  color: var(--text-white);
  outline: none;
  transition: border-color 0.2s, background 0.2s;
}
.form-input::placeholder { color: rgba(255,255,255,0.3); }
.form-input:focus {
  border-color: rgba(48,209,88,0.5);
  background: rgba(255,255,255,0.15);
}
.form-input option { background: #302b63; color: white; }
.form-select { cursor: pointer; appearance: none; }

.editor-set-block {
  margin: 0 16px 12px;
  padding: 16px;
}
.set-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 12px;
}
.set-title {
  font-size: 15px;
  font-weight: 700;
  color: var(--text-white);
}
.set-reps-control {
  display: flex;
  align-items: center;
  gap: 10px;
}
.rep-btn {
  width: 28px; height: 28px;
  border-radius: 50%;
  border: 1px solid rgba(255,255,255,0.2);
  background: rgba(255,255,255,0.1);
  color: var(--text-white);
  font-size: 16px;
  cursor: pointer;
  display: flex; align-items: center; justify-content: center;
  transition: all 0.15s;
  font-family: var(--font);
}
.rep-btn:hover { background: rgba(255,255,255,0.2); }
.rep-count {
  font-size: 15px;
  font-weight: 700;
  color: var(--text-white);
  min-width: 20px;
  text-align: center;
}

.ej-row {
  display: flex;
  gap: 8px;
  align-items: center;
  margin-bottom: 8px;
}
.ej-row .form-input { flex: 1; padding: 9px 12px; font-size: 13px; }
.ej-row .form-input.dur-input { width: 70px; flex: none; }
.ej-del-btn {
  width: 32px; height: 32px;
  border-radius: 50%;
  border: 1px solid rgba(255,69,58,0.3);
  background: rgba(255,69,58,0.1);
  color: var(--danger);
  font-size: 16px;
  cursor: pointer;
  display: flex; align-items: center; justify-content: center;
  flex-shrink: 0;
  transition: all 0.15s;
  font-family: var(--font);
}
.ej-del-btn:hover { background: rgba(255,69,58,0.2); }

.add-ej-btn {
  width: 100%;
  padding: 10px;
  background: rgba(48,209,88,0.1);
  border: 1px dashed rgba(48,209,88,0.3);
  border-radius: var(--radius-sm);
  color: var(--accent);
  font-family: var(--font);
  font-size: 13px;
  font-weight: 600;
  cursor: pointer;
  transition: all 0.2s;
  margin-top: 4px;
}
.add-ej-btn:hover { background: rgba(48,209,88,0.18); }

.editor-actions {
  padding: 0 16px 16px;
  display: flex;
  gap: 10px;
}
.btn-primary {
  flex: 1;
  padding: 14px;
  background: linear-gradient(135deg, var(--accent), #25a244);
  border: none;
  border-radius: var(--radius-md);
  color: white;
  font-family: var(--font);
  font-size: 15px;
  font-weight: 700;
  cursor: pointer;
  box-shadow: 0 4px 16px rgba(48,209,88,0.4);
  transition: all 0.2s;
}
.btn-primary:hover { transform: translateY(-1px); box-shadow: 0 6px 22px rgba(48,209,88,0.5); }
.btn-secondary {
  padding: 14px 20px;
  background: rgba(255,255,255,0.1);
  border: 1px solid rgba(255,255,255,0.2);
  border-radius: var(--radius-md);
  color: var(--text-white);
  font-family: var(--font);
  font-size: 15px;
  font-weight: 600;
  cursor: pointer;
  transition: all 0.2s;
  backdrop-filter: var(--blur-sm);
  -webkit-backdrop-filter: var(--blur-sm);
}
.btn-secondary:hover { background: rgba(255,255,255,0.18); }
.btn-danger {
  padding: 14px 20px;
  background: rgba(255,69,58,0.12);
  border: 1px solid rgba(255,69,58,0.25);
  border-radius: var(--radius-md);
  color: var(--danger);
  font-family: var(--font);
  font-size: 15px;
  font-weight: 600;
  cursor: pointer;
  transition: all 0.2s;
  backdrop-filter: var(--blur-sm);
  -webkit-backdrop-filter: var(--blur-sm);
}
.btn-danger:hover { background: rgba(255,69,58,0.22); }

.add-set-btn {
  margin: 0 16px 16px;
  width: calc(100% - 32px);
  padding: 12px;
  background: rgba(10,132,255,0.1);
  border: 1px dashed rgba(10,132,255,0.3);
  border-radius: var(--radius-md);
  color: #64b5ff;
  font-family: var(--font);
  font-size: 14px;
  font-weight: 600;
  cursor: pointer;
  transition: all 0.2s;
}
.add-set-btn:hover { background: rgba(10,132,255,0.18); }

/* ══════════ MODAL ══════════ */
.modal-overlay {
  position: fixed; inset: 0;
  background: rgba(0,0,0,0.5);
  backdrop-filter: blur(10px);
  -webkit-backdrop-filter: blur(10px);
  z-index: 200;
  display: none;
  align-items: flex-end;
  justify-content: center;
  padding: 0 0 20px;
}
.modal-overlay.open { display: flex; animation: fadeIn 0.2s ease; }
.modal-sheet {
  width: 100%;
  max-width: 480px;
  background: rgba(20,20,40,0.85);
  backdrop-filter: blur(40px) saturate(200%);
  -webkit-backdrop-filter: blur(40px) saturate(200%);
  border: 1px solid rgba(255,255,255,0.2);
  border-radius: var(--radius-xl) var(--radius-xl) var(--radius-lg) var(--radius-lg);
  padding: 20px 24px 32px;
  animation: slideUp 0.3s ease;
}
@keyframes slideUp { from { transform: translateY(60px); opacity: 0; } to { transform: translateY(0); opacity: 1; } }
.modal-handle {
  width: 36px; height: 4px;
  background: rgba(255,255,255,0.25);
  border-radius: 2px;
  margin: 0 auto 20px;
}
.modal-title {
  font-size: 20px;
  font-weight: 700;
  color: var(--text-white);
  margin-bottom: 16px;
  letter-spacing: -0.3px;
}

/* ══════════ TOAST ══════════ */
.toast {
  position: fixed;
  top: 60px;
  left: 50%; transform: translateX(-50%);
  background: rgba(48,209,88,0.25);
  backdrop-filter: blur(20px);
  -webkit-backdrop-filter: blur(20px);
  border: 1px solid rgba(48,209,88,0.4);
  border-radius: 20px;
  padding: 10px 20px;
  color: var(--text-white);
  font-size: 14px;
  font-weight: 600;
  z-index: 300;
  display: none;
  white-space: nowrap;
  box-shadow: 0 4px 16px rgba(48,209,88,0.3);
}
.toast.show { display: block; animation: toastIn 0.3s ease; }
@keyframes toastIn { from { opacity: 0; transform: translateX(-50%) translateY(-10px); } to { opacity: 1; transform: translateX(-50%) translateY(0); } }

/* ══════════ LOADING ══════════ */
.loading-screen {
  position: fixed; inset: 0;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  z-index: 400;
  background: linear-gradient(135deg, #0f0c29, #302b63, #24243e);
}
.loading-logo {
  font-size: 48px;
  margin-bottom: 16px;
}
.loading-title {
  font-size: 26px;
  font-weight: 700;
  color: var(--text-white);
  letter-spacing: -0.5px;
  margin-bottom: 8px;
}
.loading-sub {
  font-size: 14px;
  color: var(--text-white-soft);
  margin-bottom: 32px;
}
.spinner {
  width: 36px; height: 36px;
  border: 3px solid rgba(255,255,255,0.15);
  border-top-color: var(--accent);
  border-radius: 50%;
  animation: spin 0.8s linear infinite;
}
@keyframes spin { to { transform: rotate(360deg); } }

/* Responsivo desktop */
@media (min-width: 480px) {
  body {
    display: flex;
    align-items: center;
    justify-content: center;
  }
  #app {
    border-radius: 40px;
    overflow: hidden;
    box-shadow: 0 40px 80px rgba(0,0,0,0.5), 0 0 0 1px rgba(255,255,255,0.1);
    height: 844px;
    max-height: 100dvh;
  }
  .bottom-nav { position: absolute; border-radius: 0; }
  .bottom-nav { max-width: 480px; }
}

/* Fin de entrenamiento */
.finish-overlay {
  position: fixed; inset: 0;
  display: none;
  align-items: center;
  justify-content: center;
  z-index: 250;
  background: rgba(0,0,0,0.6);
  backdrop-filter: blur(20px);
  -webkit-backdrop-filter: blur(20px);
  padding: 20px;
}
.finish-overlay.open { display: flex; animation: fadeIn 0.3s ease; }
.finish-card {
  width: 100%;
  max-width: 380px;
  background: rgba(20,20,45,0.9);
  backdrop-filter: blur(40px);
  -webkit-backdrop-filter: blur(40px);
  border: 1px solid rgba(255,255,255,0.2);
  border-radius: var(--radius-xl);
  padding: 32px 28px;
  text-align: center;
}
.finish-emoji { font-size: 56px; margin-bottom: 12px; }
.finish-title {
  font-size: 26px;
  font-weight: 800;
  color: var(--text-white);
  letter-spacing: -0.5px;
  margin-bottom: 6px;
}
.finish-sub {
  font-size: 14px;
  color: var(--text-white-soft);
  margin-bottom: 24px;
}
.finish-stats {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 10px;
  margin-bottom: 24px;
}
.finish-stat {
  background: rgba(255,255,255,0.08);
  border: 1px solid rgba(255,255,255,0.12);
  border-radius: var(--radius-md);
  padding: 12px;
}
.finish-stat-val {
  font-size: 22px;
  font-weight: 700;
  color: var(--accent);
}
.finish-stat-lbl {
  font-size: 11px;
  color: var(--text-white-soft);
  margin-top: 2px;
}
.finish-notas {
  width: 100%;
  background: rgba(255,255,255,0.08);
  border: 1px solid rgba(255,255,255,0.15);
  border-radius: var(--radius-sm);
  padding: 12px 14px;
  font-family: var(--font);
  font-size: 14px;
  color: var(--text-white);
  outline: none;
  resize: none;
  margin-bottom: 16px;
}
.finish-notas::placeholder { color: rgba(255,255,255,0.3); }
</style>
</head>
<body>

<!-- Loading -->
<div class="loading-screen" id="loadingScreen">
  <div class="loading-logo">💪</div>
  <div class="loading-title">WorkoutTimer</div>
  <div class="loading-sub">Cargando tus rutinas…</div>
  <div class="spinner"></div>
</div>

<!-- Toast -->
<div class="toast" id="toast"></div>

<!-- App -->
<div id="app">

  <!-- ══ SCREEN: HOME ══ -->
  <div class="screen active" id="screen-home">
    <div class="topbar">
      <div>
        <div class="topbar-title">WorkoutTimer</div>
        <div class="topbar-sub" id="homeSubtitle">Listo para entrenar</div>
      </div>
      <button class="icon-btn" onclick="showScreen('screen-editor'); prepEditor()">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><line x1="12" y1="5" x2="12" y2="19"/><line x1="5" y1="12" x2="19" y2="12"/></svg>
      </button>
    </div>
    <div class="screen-body">
      <div class="glass greeting-card">
        <div class="greeting-text">Hola 👋</div>
        <div class="greeting-name">¿Qué entrenamos hoy?</div>
        <div class="streak-row">
          <div class="streak-pill" id="streakPill">
            <svg viewBox="0 0 24 24" fill="currentColor"><path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm0 18c-4.41 0-8-3.59-8-8s3.59-8 8-8 8 3.59 8 8-3.59 8-8 8zm.5-13H11v6l5.25 3.15.75-1.23-4.5-2.67V7z"/></svg>
            <span id="streakText">Cargando…</span>
          </div>
        </div>
      </div>

      <div class="section-label">Mis rutinas</div>
      <div id="rutinasList"></div>
    </div>
  </div>

  <!-- ══ SCREEN: TIMER ══ -->
  <div class="screen" id="screen-timer">
    <div class="topbar" style="padding-top:56px">
      <button class="icon-btn" onclick="stopTimer(); showScreen('screen-home')">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><polyline points="15 18 9 12 15 6"/></svg>
      </button>
      <div style="text-align:center">
        <div class="timer-rutina-name" id="timerRutinaName">—</div>
        <div class="timer-set-badge" id="timerSetBadge">Set 1</div>
      </div>
      <div style="width:40px"></div>
    </div>
    <div class="screen-body" style="padding-top:8px">
      <!-- Ring -->
      <div class="timer-ring-wrap">
        <div class="timer-ring-container">
          <svg viewBox="0 0 240 240">
            <circle class="ring-bg" cx="120" cy="120" r="108"/>
            <circle class="ring-progress" id="ringProgress" cx="120" cy="120" r="108"
              stroke-dasharray="678.58" stroke-dashoffset="0"/>
          </svg>
          <div class="timer-ring-inner">
            <div class="timer-countdown" id="timerCountdown">00:00</div>
            <div class="timer-ejercicio-name" id="timerEjNombre">—</div>
          </div>
        </div>
      </div>

      <!-- Siguiente -->
      <div class="glass-sm timer-ejercicio-card">
        <div class="timer-ej-actual" id="timerEjActual">—</div>
        <div class="timer-ej-siguiente">Siguiente: <span id="timerEjSiguiente">—</span></div>
      </div>

      <!-- Progreso global -->
      <div class="timer-progress-bar-wrap">
        <div class="timer-progress-label">
          <span id="progressLabel">Ejercicio 0 / 0</span>
          <span id="progressPct">0%</span>
        </div>
        <div class="timer-progress-bar"><div class="timer-progress-fill" id="progressFill" style="width:0%"></div></div>
      </div>

      <!-- Controles -->
      <div class="timer-controls">
        <button class="btn-control secondary" onclick="prevEjercicio()" title="Anterior">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><polyline points="15 18 9 12 15 6"/></svg>
        </button>
        <button class="btn-control primary" id="btnPlayPause" onclick="togglePlayPause()">
          <svg id="iconPlay" viewBox="0 0 24 24" fill="currentColor"><polygon points="5 3 19 12 5 21 5 3"/></svg>
          <svg id="iconPause" viewBox="0 0 24 24" fill="currentColor" style="display:none"><rect x="6" y="4" width="4" height="16"/><rect x="14" y="4" width="4" height="16"/></svg>
        </button>
        <button class="btn-control secondary" onclick="nextEjercicio()" title="Siguiente">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><polyline points="9 18 15 12 9 6"/></svg>
        </button>
      </div>

      <!-- Cola de ejercicios -->
      <div class="glass-sm timer-queue">
        <div class="queue-title">En cola</div>
        <div id="timerQueue"></div>
      </div>
    </div>
  </div>

  <!-- ══ SCREEN: HISTORIAL ══ -->
  <div class="screen" id="screen-historial">
    <div class="topbar">
      <div>
        <div class="topbar-title">Historial</div>
        <div class="topbar-sub">Tus entrenamientos</div>
      </div>
    </div>
    <div class="screen-body">
      <div class="stats-row">
        <div class="glass-sm stat-card">
          <div class="stat-value stat-accent" id="statTotal">0</div>
          <div class="stat-label">Entrenamientos</div>
        </div>
        <div class="glass-sm stat-card">
          <div class="stat-value stat-accent" id="statMinutos">0</div>
          <div class="stat-label">Minutos totales</div>
        </div>
      </div>
      <div class="stats-row">
        <div class="glass-sm stat-card">
          <div class="stat-value stat-accent" id="statSemana">0</div>
          <div class="stat-label">Esta semana</div>
        </div>
        <div class="glass-sm stat-card">
          <div class="stat-value stat-accent" id="statRacha">0</div>
          <div class="stat-label">Racha actual 🔥</div>
        </div>
      </div>
      <div class="section-label">Recientes</div>
      <div id="historialList"></div>
    </div>
  </div>

  <!-- ══ SCREEN: CALENDARIO ══ -->
  <div class="screen" id="screen-calendario">
    <div class="topbar">
      <div>
        <div class="topbar-title">Calendario</div>
        <div class="topbar-sub" id="calSubtitle">—</div>
      </div>
    </div>
    <div class="screen-body" style="padding:0 0 100px">
      <div class="cal-nav" style="padding:0 20px 12px">
        <button class="icon-btn" onclick="changeMonth(-1)">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><polyline points="15 18 9 12 15 6"/></svg>
        </button>
        <div class="cal-month" id="calMonthLabel">—</div>
        <button class="icon-btn" onclick="changeMonth(1)">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><polyline points="9 18 15 12 9 6"/></svg>
        </button>
      </div>
      <div class="glass cal-card">
        <div class="cal-days-header">
          <div class="cal-day-header">L</div>
          <div class="cal-day-header">M</div>
          <div class="cal-day-header">X</div>
          <div class="cal-day-header">J</div>
          <div class="cal-day-header">V</div>
          <div class="cal-day-header">S</div>
          <div class="cal-day-header">D</div>
        </div>
        <div class="cal-grid" id="calGrid"></div>
      </div>
      <div class="glass-sm cal-detail" id="calDetail" style="display:none">
        <div class="cal-detail-title" id="calDetailTitle">—</div>
        <div id="calDetailList"></div>
      </div>
    </div>
  </div>

  <!-- ══ SCREEN: EDITOR ══ -->
  <div class="screen" id="screen-editor">
    <div class="topbar">
      <button class="icon-btn" onclick="showScreen('screen-home')">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><polyline points="15 18 9 12 15 6"/></svg>
      </button>
      <div>
        <div class="topbar-title">Editor</div>
        <div class="topbar-sub">Crear o editar rutina</div>
      </div>
      <div style="width:40px"></div>
    </div>
    <div class="screen-body" style="padding-top:8px">
      <!-- Seleccionar o nueva rutina -->
      <div class="glass-sm editor-rutina-select" style="padding:16px;margin:0 16px 16px">
        <div class="form-group">
          <label class="form-label">Rutina</label>
          <select class="form-input form-select" id="editorRutinaSelect" onchange="loadRutinaEnEditor()">
            <option value="__nueva__">+ Nueva rutina</option>
          </select>
        </div>
        <div class="form-group" id="editorNombreWrap">
          <label class="form-label">Nombre</label>
          <input class="form-input" id="editorNombre" placeholder="Ej: Abs, Full Body, Piernas…" type="text">
        </div>
      </div>

      <!-- Sets -->
      <div id="editorSets"></div>
      <button class="add-set-btn" onclick="addSet()">+ Agregar Set</button>

      <!-- Acciones -->
      <div class="editor-actions">
        <button class="btn-primary" onclick="guardarRutina()">Guardar rutina</button>
        <button class="btn-danger" id="btnEliminarRutina" onclick="eliminarRutina()" style="display:none">🗑</button>
      </div>
    </div>
  </div>

  <!-- BOTTOM NAV -->
  <nav class="bottom-nav">
    <div class="nav-bar">
      <div class="nav-item active" onclick="navTo('screen-home')">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M3 9l9-7 9 7v11a2 2 0 01-2 2H5a2 2 0 01-2-2z"/><polyline points="9 22 9 12 15 12 15 22"/></svg>
        <span class="nav-label">Inicio</span>
      </div>
      <div class="nav-item" onclick="navTo('screen-historial'); loadHistorial()">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="22 12 18 12 15 21 9 3 6 12 2 12"/></svg>
        <span class="nav-label">Historial</span>
      </div>
      <div class="nav-item" onclick="navTo('screen-calendario'); renderCalendario()">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="3" y="4" width="18" height="18" rx="2" ry="2"/><line x1="16" y1="2" x2="16" y2="6"/><line x1="8" y1="2" x2="8" y2="6"/><line x1="3" y1="10" x2="21" y2="10"/></svg>
        <span class="nav-label">Calendario</span>
      </div>
      <div class="nav-item" onclick="navTo('screen-editor'); prepEditor()">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M11 4H4a2 2 0 00-2 2v14a2 2 0 002 2h14a2 2 0 002-2v-7"/><path d="M18.5 2.5a2.121 2.121 0 013 3L12 15l-4 1 1-4 9.5-9.5z"/></svg>
        <span class="nav-label">Editor</span>
      </div>
    </div>
  </nav>
</div>

<!-- Modal: fin de entrenamiento -->
<div class="finish-overlay" id="finishOverlay">
  <div class="finish-card">
    <div class="finish-emoji">🎉</div>
    <div class="finish-title">¡Listo!</div>
    <div class="finish-sub">Entrenamiento completado</div>
    <div class="finish-stats">
      <div class="finish-stat">
        <div class="finish-stat-val" id="finishDur">0:00</div>
        <div class="finish-stat-lbl">Duración</div>
      </div>
      <div class="finish-stat">
        <div class="finish-stat-val" id="finishRutina">—</div>
        <div class="finish-stat-lbl">Rutina</div>
      </div>
    </div>
    <textarea class="finish-notas" id="finishNotas" rows="2" placeholder="¿Cómo te sentiste? (opcional)"></textarea>
    <button class="btn-primary" onclick="guardarEntrenamiento()" style="width:100%">Guardar 💾</button>
    <button class="btn-secondary" onclick="cerrarFinish()" style="width:100%;margin-top:8px">Descartar</button>
  </div>
</div>

<script>
const SCRIPT_URL = 'https://script.google.com/macros/s/AKfycbyX0a0SYzFJoRysSGNbrXZ70zUv6UMQI-GiLrCCfwRbPu_jGartBQ0VZrVIP5gJ7gGQeQ/exec';

// ══════════ STATE ══════════
let rutinas = [];
let entrenamientos = [];
let currentScreen = 'screen-home';

// Timer state
let timerRutina = null;
let timerExercicios = []; // lista plana de todos los ejercicios del workout
let timerIdx = 0;
let timerSegsTotal = 0;
let timerSegsRestantes = 0;
let timerInterval = null;
let timerPlaying = false;
let timerStartTime = null;
let timerElapsedSecs = 0;
let timerRepActual = 0;
let timerReps = 1;

// Calendario
let calYear = new Date().getFullYear();
let calMonth = new Date().getMonth();

// ══════════ INIT ══════════
async function init() {
  try {
    const [dataRutinas, dataEnt] = await Promise.all([
      apiFetch('getRutinas'),
      apiFetch('getEntrenamientos')
    ]);
    rutinas = dataRutinas.rutinas || [];
    entrenamientos = dataEnt.entrenamientos || [];
    renderHome();
    document.getElementById('loadingScreen').style.display = 'none';
  } catch(e) {
    console.error(e);
    document.getElementById('loadingScreen').innerHTML = `
      <div style="text-align:center;padding:40px;color:white">
        <div style="font-size:40px;margin-bottom:12px">⚠️</div>
        <div style="font-size:18px;font-weight:700;margin-bottom:8px">Sin conexión</div>
        <div style="font-size:13px;opacity:0.6">Verificá el Apps Script y volvé a intentar</div>
        <button onclick="init()" style="margin-top:20px;padding:10px 24px;border-radius:20px;border:none;background:#30d158;color:white;font-size:14px;font-weight:700;cursor:pointer;font-family:inherit">Reintentar</button>
      </div>`;
  }
}

async function apiFetch(accion, method = 'GET', body = null) {
  const url = method === 'GET' ? `${SCRIPT_URL}?accion=${accion}` : SCRIPT_URL;
  const opts = method === 'GET' ? {} : {
    method: 'POST',
    headers: { 'Content-Type': 'text/plain' },
    body: JSON.stringify({ accion, ...body })
  };
  const r = await fetch(url, opts);
  const data = await r.json();
  if (!data.ok) throw new Error(data.error || 'Error');
  return data;
}

// ══════════ SCREENS ══════════
function showScreen(id) {
  document.querySelectorAll('.screen').forEach(s => s.classList.remove('active'));
  document.getElementById(id).classList.add('active');
  currentScreen = id;
}

function navTo(id) {
  showScreen(id);
  document.querySelectorAll('.nav-item').forEach((n, i) => {
    n.classList.remove('active');
  });
  const map = ['screen-home','screen-historial','screen-calendario','screen-editor'];
  const idx = map.indexOf(id);
  if (idx > -1) document.querySelectorAll('.nav-item')[idx].classList.add('active');
}

// ══════════ HOME ══════════
function renderHome() {
  // Streak
  const hoy = new Date().toISOString().split('T')[0];
  const racha = calcRacha();
  document.getElementById('streakText').textContent = racha > 0 ? `${racha} día${racha>1?'s':''} seguidos 🔥` : 'Sin racha aún';
  document.getElementById('homeSubtitle').textContent = entrenamientos.length > 0
    ? `Último: ${entrenamientos[entrenamientos.length-1]?.rutina || '—'}`
    : 'Tu primera sesión te espera';

  const list = document.getElementById('rutinasList');
  if (rutinas.length === 0) {
    list.innerHTML = `<div class="empty-state">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm0 18c-4.41 0-8-3.59-8-8s3.59-8 8-8 8 3.59 8 8-3.59 8-8 8zm.5-13H11v6l5.25 3.15.75-1.23-4.5-2.67V7z"/></svg>
      <p>No hay rutinas aún.<br>Creá una en el Editor ✏️</p>
    </div>`;
    return;
  }
  list.innerHTML = rutinas.map(r => {
    const ejercicios = Object.values(r.sets).flat();
    const durTotal = ejercicios.reduce((a, e) => a + e.duracion, 0);
    const nSets = Object.keys(r.sets).length;
    return `<div class="glass-sm rutina-card" onclick="iniciarRutina('${r.nombre}')">
      <div class="rutina-info">
        <div class="rutina-name">${r.nombre}</div>
        <div class="rutina-meta">${nSets} set${nSets>1?'s':''} · ${ejercicios.length} ejercicios · ${Math.ceil(durTotal/60)} min</div>
      </div>
      <div class="rutina-arrow">
        <svg viewBox="0 0 24 24" fill="currentColor"><polygon points="5 3 19 12 5 21 5 3"/></svg>
      </div>
    </div>`;
  }).join('');
}

// ══════════ TIMER ══════════
function iniciarRutina(nombreRutina) {
  timerRutina = rutinas.find(r => r.nombre === nombreRutina);
  if (!timerRutina) return;

  // Construir lista plana de ejercicios (setsxreps)
  timerExercicios = [];
  const sets = Object.keys(timerRutina.sets).sort();
  for (const set of sets) {
    const ejsSet = timerRutina.sets[set];
    timerExercicios.push({ tipo: '__set_header__', nombre: set });
    for (const ej of ejsSet) {
      timerExercicios.push({ ...ej, set });
    }
  }
  // Filtrar headers para el índice real
  timerExercicios = timerExercicios.filter(e => e.tipo !== '__set_header__');

  timerIdx = 0;
  timerPlaying = false;
  timerStartTime = null;
  timerElapsedSecs = 0;
  clearInterval(timerInterval);

  showScreen('screen-timer');
  renderTimerUI();
}

function renderTimerUI() {
  const ej = timerExercicios[timerIdx];
  if (!ej) return;

  document.getElementById('timerRutinaName').textContent = timerRutina.nombre;
  document.getElementById('timerSetBadge').textContent = ej.set;

  timerSegsTotal = ej.duracion;
  timerSegsRestantes = ej.duracion;

  updateTimerDisplay();
  updateQueue();
  updateProgress();

  // Siguiente
  const sig = timerExercicios[timerIdx + 1];
  document.getElementById('timerEjSiguiente').textContent = sig ? `${sig.ejercicio} (${sig.duracion}s)` : '¡Fin!';
}

function updateTimerDisplay() {
  const ej = timerExercicios[timerIdx];
  const isRest = ej?.ejercicio?.toLowerCase().includes('rest') || ej?.ejercicio?.toLowerCase().includes('descanso');
  const secs = timerSegsRestantes;
  const m = Math.floor(secs / 60).toString().padStart(2, '0');
  const s = (secs % 60).toString().padStart(2, '0');
  document.getElementById('timerCountdown').textContent = `${m}:${s}`;
  document.getElementById('timerEjNombre').textContent = ej?.ejercicio || '—';
  document.getElementById('timerEjActual').textContent = ej?.ejercicio || '—';

  // Ring
  const circum = 2 * Math.PI * 108; // 678.58
  const pct = timerSegsRestantes / timerSegsTotal;
  const offset = circum * (1 - pct);
  const ring = document.getElementById('ringProgress');
  ring.style.strokeDashoffset = offset;
  ring.className = 'ring-progress' + (isRest ? ' rest' : '');
}

function updateProgress() {
  const total = timerExercicios.length;
  const done = timerIdx;
  const pct = Math.round((done / total) * 100);
  document.getElementById('progressLabel').textContent = `Ejercicio ${done + 1} / ${total}`;
  document.getElementById('progressPct').textContent = `${pct}%`;
  document.getElementById('progressFill').style.width = pct + '%';
}

function updateQueue() {
  const queue = document.getElementById('timerQueue');
  const start = Math.max(0, timerIdx - 1);
  const end = Math.min(timerExercicios.length, timerIdx + 5);
  queue.innerHTML = timerExercicios.slice(start, end).map((e, i) => {
    const realIdx = start + i;
    const isRest = e.ejercicio?.toLowerCase().includes('rest') || e.ejercicio?.toLowerCase().includes('descanso');
    let cls, dotCls;
    if (realIdx < timerIdx) { cls = 'done'; dotCls = 'done-dot'; }
    else if (realIdx === timerIdx) { cls = 'current'; dotCls = isRest ? 'rest-dot' : 'active'; }
    else { cls = 'upcoming'; dotCls = isRest ? 'rest-dot' : 'up-dot'; }
    return `<div class="queue-item ${cls}">
      <div class="queue-dot ${dotCls}"></div>
      <div class="queue-item-name">${e.ejercicio}</div>
      <div class="queue-item-time">${e.duracion}s</div>
    </div>`;
  }).join('');
}

function togglePlayPause() {
  if (timerPlaying) {
    pauseTimer();
  } else {
    playTimer();
  }
}

function playTimer() {
  timerPlaying = true;
  if (!timerStartTime) timerStartTime = Date.now();
  document.getElementById('iconPlay').style.display = 'none';
  document.getElementById('iconPause').style.display = 'block';

  // Anunciar ejercicio al iniciar
  if (timerSegsRestantes === timerExercicios[timerIdx]?.duracion) {
    hablar(timerExercicios[timerIdx]?.ejercicio || '');
  }

  timerInterval = setInterval(() => {
    if (timerSegsRestantes > 0) {
      timerSegsRestantes--;
      timerElapsedSecs++;
      updateTimerDisplay();

      // Beep en últimos 3 segundos
      if (timerSegsRestantes <= 3 && timerSegsRestantes > 0) {
        beep(timerSegsRestantes === 1 ? 880 : 660, 100);
      }
    } else {
      nextEjercicio();
    }
  }, 1000);
}

function pauseTimer() {
  timerPlaying = false;
  clearInterval(timerInterval);
  document.getElementById('iconPlay').style.display = 'block';
  document.getElementById('iconPause').style.display = 'none';
}

function stopTimer() {
  pauseTimer();
  timerIdx = 0;
  timerElapsedSecs = 0;
}

function nextEjercicio() {
  clearInterval(timerInterval);
  timerPlaying = false;
  document.getElementById('iconPlay').style.display = 'block';
  document.getElementById('iconPause').style.display = 'none';

  if (timerIdx < timerExercicios.length - 1) {
    timerIdx++;
    renderTimerUI();
    beep(1047, 150); // do agudo
    setTimeout(() => {
      const ej = timerExercicios[timerIdx];
      hablar(`${ej.ejercicio}, ${ej.duracion} segundos`);
      playTimer();
    }, 600);
  } else {
    // Fin del entrenamiento
    beep(1047, 200);
    setTimeout(() => beep(1319, 200), 250);
    setTimeout(() => beep(1568, 400), 500);
    hablar('¡Entrenamiento completado! ¡Muy bien!');
    mostrarFinish();
  }
}

function prevEjercicio() {
  clearInterval(timerInterval);
  timerPlaying = false;
  document.getElementById('iconPlay').style.display = 'block';
  document.getElementById('iconPause').style.display = 'none';
  if (timerIdx > 0) {
    timerIdx--;
    renderTimerUI();
  }
}

// ══════════ VOZ ══════════
function hablar(texto) {
  if (!window.speechSynthesis) return;
  window.speechSynthesis.cancel();
  const u = new SpeechSynthesisUtterance(texto);
  u.lang = 'es-AR';
  u.rate = 0.95;
  u.pitch = 1.0;
  window.speechSynthesis.speak(u);
}

// ══════════ BEEP ══════════
let audioCtx = null;
function beep(freq = 880, dur = 150) {
  try {
    if (!audioCtx) audioCtx = new (window.AudioContext || window.webkitAudioContext)();
    const osc = audioCtx.createOscillator();
    const gain = audioCtx.createGain();
    osc.connect(gain); gain.connect(audioCtx.destination);
    osc.frequency.value = freq;
    osc.type = 'sine';
    gain.gain.setValueAtTime(0.3, audioCtx.currentTime);
    gain.gain.exponentialRampToValueAtTime(0.001, audioCtx.currentTime + dur / 1000);
    osc.start(); osc.stop(audioCtx.currentTime + dur / 1000);
  } catch(e) {}
}

// ══════════ FIN ENTRENAMIENTO ══════════
function mostrarFinish() {
  const dur = timerElapsedSecs;
  const m = Math.floor(dur / 60).toString().padStart(2,'0');
  const s = (dur % 60).toString().padStart(2,'0');
  document.getElementById('finishDur').textContent = `${m}:${s}`;
  document.getElementById('finishRutina').textContent = timerRutina?.nombre || '—';
  document.getElementById('finishNotas').value = '';
  document.getElementById('finishOverlay').classList.add('open');
}

function cerrarFinish() {
  document.getElementById('finishOverlay').classList.remove('open');
  showScreen('screen-home');
}

async function guardarEntrenamiento() {
  const notas = document.getElementById('finishNotas').value;
  const btn = document.querySelector('#finishOverlay .btn-primary');
  btn.textContent = 'Guardando…';
  btn.disabled = true;

  try {
    await apiFetch('guardarEntrenamiento', 'POST', {
      rutina: timerRutina.nombre,
      duracionTotal: timerElapsedSecs,
      notas
    });
    // Actualizar local
    const hoy = new Date();
    entrenamientos.push({
      fecha: hoy.toISOString().split('T')[0],
      hora: hoy.toTimeString().slice(0,5),
      rutina: timerRutina.nombre,
      duracionTotal: timerElapsedSecs,
      duracionFormateada: `${Math.floor(timerElapsedSecs/60).toString().padStart(2,'0')}:${(timerElapsedSecs%60).toString().padStart(2,'0')}`,
      notas
    });
    showToast('✅ ¡Entrenamiento guardado!');
    cerrarFinish();
    renderHome();
  } catch(e) {
    showToast('❌ Error al guardar');
    btn.textContent = 'Guardar 💾';
    btn.disabled = false;
  }
}

// ══════════ HISTORIAL ══════════
function loadHistorial() {
  const total = entrenamientos.length;
  const minutos = entrenamientos.reduce((a, e) => a + Math.round(e.duracionTotal / 60), 0);
  const hoy = new Date();
  const semanaActual = getISOWeek(hoy);
  const semana = entrenamientos.filter(e => {
    const f = new Date(e.fecha + 'T12:00:00');
    return getISOWeek(f) === semanaActual && f.getFullYear() === hoy.getFullYear();
  }).length;

  document.getElementById('statTotal').textContent = total;
  document.getElementById('statMinutos').textContent = minutos;
  document.getElementById('statSemana').textContent = semana;
  document.getElementById('statRacha').textContent = calcRacha();

  const list = document.getElementById('historialList');
  if (total === 0) {
    list.innerHTML = `<div class="empty-state"><p>Aún no hay entrenamientos guardados.</p></div>`;
    return;
  }

  const sorted = [...entrenamientos].reverse();
  list.innerHTML = sorted.map(e => {
    const dur = Math.round(e.duracionTotal / 60);
    const fecha = new Date(e.fecha + 'T12:00:00');
    const fechaStr = fecha.toLocaleDateString('es-AR', { weekday: 'short', day: 'numeric', month: 'short' });
    return `<div class="glass-sm hist-item">
      <div class="hist-dot">💪</div>
      <div class="hist-info">
        <div class="hist-rutina">${e.rutina}</div>
        <div class="hist-fecha">${fechaStr} · ${e.hora}</div>
        ${e.notas ? `<div class="hist-notas">"${e.notas}"</div>` : ''}
      </div>
      <div class="hist-duracion">${dur} min</div>
    </div>`;
  }).join('');
}

// ══════════ CALENDARIO ══════════
function renderCalendario() {
  const meses = ['Enero','Febrero','Marzo','Abril','Mayo','Junio','Julio','Agosto','Septiembre','Octubre','Noviembre','Diciembre'];
  document.getElementById('calMonthLabel').textContent = `${meses[calMonth]} ${calYear}`;
  const entrenadosEste = entrenamientos.filter(e => {
    const f = new Date(e.fecha + 'T12:00:00');
    return f.getMonth() === calMonth && f.getFullYear() === calYear;
  }).length;
  document.getElementById('calSubtitle').textContent = `${entrenadosEste} sesión${entrenadosEste!==1?'es':''} este mes`;

  const grid = document.getElementById('calGrid');
  const primerDia = new Date(calYear, calMonth, 1);
  const ultimoDia = new Date(calYear, calMonth + 1, 0);
  const hoy = new Date();

  // Offset: lunes = 0
  let offset = primerDia.getDay() - 1;
  if (offset < 0) offset = 6;

  // Entrenamientos por fecha
  const byFecha = {};
  entrenamientos.forEach(e => {
    if (!byFecha[e.fecha]) byFecha[e.fecha] = [];
    byFecha[e.fecha].push(e);
  });

  let cells = '';
  // Días del mes anterior
  const prevMonth = new Date(calYear, calMonth, 0);
  for (let i = offset - 1; i >= 0; i--) {
    cells += `<div class="cal-cell other-month"><span class="cal-cell-num">${prevMonth.getDate() - i}</span></div>`;
  }
  // Días del mes
  for (let d = 1; d <= ultimoDia.getDate(); d++) {
    const fecha = `${calYear}-${String(calMonth+1).padStart(2,'0')}-${String(d).padStart(2,'0')}`;
    const isHoy = hoy.getDate()===d && hoy.getMonth()===calMonth && hoy.getFullYear()===calYear;
    const tieneWorkout = byFecha[fecha]?.length > 0;
    const cls = [isHoy ? 'today' : '', tieneWorkout ? 'has-workout' : ''].filter(Boolean).join(' ');
    cells += `<div class="cal-cell ${cls}" onclick="${tieneWorkout ? `showCalDetail('${fecha}')` : ''}">
      <span class="cal-cell-num">${d}</span>
      ${tieneWorkout ? '<div class="cal-dot"></div>' : ''}
    </div>`;
  }
  // Rellenar
  const total = offset + ultimoDia.getDate();
  const rem = total % 7 === 0 ? 0 : 7 - (total % 7);
  for (let i = 1; i <= rem; i++) {
    cells += `<div class="cal-cell other-month"><span class="cal-cell-num">${i}</span></div>`;
  }
  grid.innerHTML = cells;
}

function changeMonth(dir) {
  calMonth += dir;
  if (calMonth > 11) { calMonth = 0; calYear++; }
  if (calMonth < 0) { calMonth = 11; calYear--; }
  renderCalendario();
  document.getElementById('calDetail').style.display = 'none';
}

function showCalDetail(fecha) {
  const ents = entrenamientos.filter(e => e.fecha === fecha);
  const f = new Date(fecha + 'T12:00:00');
  const label = f.toLocaleDateString('es-AR', { weekday: 'long', day: 'numeric', month: 'long' });
  document.getElementById('calDetailTitle').textContent = label;
  document.getElementById('calDetailList').innerHTML = ents.map(e => `
    <div class="cal-detail-item">
      <div style="font-size:20px">💪</div>
      <div>
        <div style="font-size:14px;font-weight:600;color:var(--text-white)">${e.rutina}</div>
        <div style="font-size:12px;color:var(--text-white-soft)">${e.hora} · ${Math.round(e.duracionTotal/60)} min${e.notas ? ` · "${e.notas}"` : ''}</div>
      </div>
    </div>`).join('');
  const detail = document.getElementById('calDetail');
  detail.style.display = 'block';
  detail.scrollIntoView({ behavior: 'smooth' });
}

// ══════════ EDITOR ══════════
function prepEditor() {
  const sel = document.getElementById('editorRutinaSelect');
  sel.innerHTML = '<option value="__nueva__">+ Nueva rutina</option>';
  rutinas.forEach(r => {
    const o = document.createElement('option');
    o.value = r.nombre;
    o.textContent = r.nombre;
    sel.appendChild(o);
  });
  sel.value = '__nueva__';
  loadRutinaEnEditor();
}

function loadRutinaEnEditor() {
  const val = document.getElementById('editorRutinaSelect').value;
  const nombreWrap = document.getElementById('editorNombreWrap');
  const btnEl = document.getElementById('btnEliminarRutina');

  if (val === '__nueva__') {
    nombreWrap.style.display = 'block';
    document.getElementById('editorNombre').value = '';
    btnEl.style.display = 'none';
    renderEditorSets([{ nombre: 'Set 1', ejercicios: [{ ejercicio: '', duracion: 40 }] }]);
  } else {
    nombreWrap.style.display = 'none';
    btnEl.style.display = 'block';
    const r = rutinas.find(x => x.nombre === val);
    if (r) {
      const sets = Object.keys(r.sets).sort().map(s => ({
        nombre: s,
        ejercicios: r.sets[s].map(e => ({ ejercicio: e.ejercicio, duracion: e.duracion }))
      }));
      renderEditorSets(sets);
    }
  }
}

let editorSetsData = [];

function renderEditorSets(sets) {
  editorSetsData = sets;
  const container = document.getElementById('editorSets');
  container.innerHTML = editorSetsData.map((set, si) => renderSetBlock(set, si)).join('');
}

function renderSetBlock(set, si) {
  const ejsHTML = set.ejercicios.map((e, ei) => `
    <div class="ej-row">
      <input class="form-input" placeholder="Ejercicio" value="${e.ejercicio}" onchange="editorSetsData[${si}].ejercicios[${ei}].ejercicio=this.value">
      <input class="form-input dur-input" type="number" min="1" value="${e.duracion}" placeholder="seg" onchange="editorSetsData[${si}].ejercicios[${ei}].duracion=Number(this.value)">
      <button class="ej-del-btn" onclick="deleteEj(${si},${ei})">✕</button>
    </div>`).join('');
  return `<div class="glass-sm editor-set-block" style="padding:16px;margin:0 16px 12px">
    <div class="set-header">
      <div class="set-title">${set.nombre}</div>
      <button onclick="deleteSet(${si})" style="background:rgba(255,69,58,0.1);border:1px solid rgba(255,69,58,0.25);border-radius:8px;color:var(--danger);padding:4px 10px;font-size:12px;font-weight:600;cursor:pointer;font-family:inherit">Eliminar set</button>
    </div>
    ${ejsHTML}
    <button class="add-ej-btn" onclick="addEjercicio(${si})">+ Ejercicio</button>
  </div>`;
}

function addEjercicio(si) {
  editorSetsData[si].ejercicios.push({ ejercicio: '', duracion: 40 });
  document.getElementById('editorSets').innerHTML = editorSetsData.map((s, i) => renderSetBlock(s, i)).join('');
}

function deleteEj(si, ei) {
  editorSetsData[si].ejercicios.splice(ei, 1);
  document.getElementById('editorSets').innerHTML = editorSetsData.map((s, i) => renderSetBlock(s, i)).join('');
}

function addSet() {
  editorSetsData.push({ nombre: `Set ${editorSetsData.length + 1}`, ejercicios: [{ ejercicio: '', duracion: 40 }] });
  document.getElementById('editorSets').innerHTML = editorSetsData.map((s, i) => renderSetBlock(s, i)).join('');
}

function deleteSet(si) {
  editorSetsData.splice(si, 1);
  editorSetsData.forEach((s, i) => s.nombre = `Set ${i + 1}`);
  document.getElementById('editorSets').innerHTML = editorSetsData.map((s, i) => renderSetBlock(s, i)).join('');
}

async function guardarRutina() {
  const val = document.getElementById('editorRutinaSelect').value;
  const esNueva = val === '__nueva__';
  const nombre = esNueva ? document.getElementById('editorNombre').value.trim() : val;

  if (!nombre) return showToast('⚠️ Poné un nombre a la rutina');
  if (editorSetsData.length === 0) return showToast('⚠️ Agregá al menos un set');

  // Construir filas para Sheets
  const filas = [];
  const id = nombre.toLowerCase().replace(/\s+/g, '-') + '-001';
  let orden = 1;
  for (const set of editorSetsData) {
    for (const ej of set.ejercicios) {
      if (!ej.ejercicio.trim()) continue;
      filas.push({ id, nombre, set: set.nombre, ejercicio: ej.ejercicio, duracion: ej.duracion, orden });
      orden++;
    }
  }
  if (filas.length === 0) return showToast('⚠️ Completá al menos un ejercicio');

  try {
    await apiFetch('guardarRutina', 'POST', { filas, nombre, esNueva });
    // Actualizar local
    const sets = {};
    editorSetsData.forEach(s => {
      sets[s.nombre] = s.ejercicios.filter(e => e.ejercicio.trim()).map((e, i) => ({
        ejercicio: e.ejercicio, duracion: e.duracion, orden: i + 1
      }));
    });
    if (esNueva) {
      rutinas.push({ id, nombre, sets });
    } else {
      const idx = rutinas.findIndex(r => r.nombre === nombre);
      if (idx > -1) rutinas[idx].sets = sets;
    }
    renderHome();
    showToast('✅ Rutina guardada');
    setTimeout(() => navTo('screen-home'), 800);
  } catch(e) {
    showToast('❌ Error al guardar: ' + e.message);
  }
}

async function eliminarRutina() {
  const nombre = document.getElementById('editorRutinaSelect').value;
  if (!confirm(`¿Eliminar la rutina "${nombre}"?`)) return;
  try {
    await apiFetch('eliminarRutina', 'POST', { nombre });
    rutinas = rutinas.filter(r => r.nombre !== nombre);
    renderHome();
    prepEditor();
    showToast('🗑 Rutina eliminada');
  } catch(e) {
    showToast('❌ Error: ' + e.message);
  }
}

// ══════════ HELPERS ══════════
function calcRacha() {
  if (entrenamientos.length === 0) return 0;
  const fechas = [...new Set(entrenamientos.map(e => e.fecha))].sort().reverse();
  const hoy = new Date().toISOString().split('T')[0];
  let racha = 0, check = hoy;
  for (const f of fechas) {
    if (f === check) {
      racha++;
      const d = new Date(check + 'T12:00:00');
      d.setDate(d.getDate() - 1);
      check = d.toISOString().split('T')[0];
    } else break;
  }
  return racha;
}

function getISOWeek(d) {
  const date = new Date(Date.UTC(d.getFullYear(), d.getMonth(), d.getDate()));
  date.setUTCDate(date.getUTCDate() + 4 - (date.getUTCDay() || 7));
  const yearStart = new Date(Date.UTC(date.getUTCFullYear(), 0, 1));
  return Math.ceil((((date - yearStart) / 86400000) + 1) / 7);
}

function showToast(msg) {
  const t = document.getElementById('toast');
  t.textContent = msg;
  t.classList.add('show');
  setTimeout(() => t.classList.remove('show'), 2800);
}

// ══════════ START ══════════
init();
</script>
</body>
</html>
